PORT=5000
JWT_SECRET=super_secret_key_for_url_shortener_2026_db_postgres_123!

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=root@123
DB_NAME=url_shortener

# Alternative single connection URL (uncomment to use)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/url_shortener
FORCE_DB_SYNC=false
# Update these to your ngrok public URL when tunneling
BASE_URL=https://chemo-backstage-reissue.ngrok-free.dev
FRONTEND_URL=http://localhost:5174
# If your frontend is also served publicly via ngrok, set this too:
FRONTEND_NGROK_URL=https://your-frontend-ngrok-subdomain.ngrok-free.app

# Nodemailer — Gmail credentials for password reset emails
# Use an App Password if 2FA is enabled on your Gmail account
EMAIL_USER=kanikasuresh62@gmail.com
EMAIL_PASS=wixn kwdi kxsn bcnm
